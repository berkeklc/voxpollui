import 'package:flutter/material.dart';

class CommunityPrivate extends StatelessWidget {
  const CommunityPrivate({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('');
  }
}
